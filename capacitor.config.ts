import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Security_Home',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
